<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Admin Panel</title>
    <!-- Bootstrap CSS -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha3/dist/css/bootstrap.min.css" rel="stylesheet">
    <style>
        /* General Styles */
        body {
            font-family: 'Arial', sans-serif;
            background-color: #f9fafe;
            margin: 0;
            padding: 0;
        }

        a {
            text-decoration: none;
        }

        /* Header */
        .header {
            background: linear-gradient(to right, #4facfe, #00f2fe);
            color: white;
            padding: 15px 30px;
            display: flex;
            align-items: center;
            justify-content: space-between;
            box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
        }

        .header .logo {
            font-size: 1.8rem;
            font-weight: bold;
            letter-spacing: 2px;
        }

        .header .logout-btn {
            background: linear-gradient(to right, #ff7eb3, #ff758c);
            color: white;
            border: none;
            border-radius: 10px;
            padding: 8px 15px;
            font-size: 0.9rem;
            transition: transform 0.3s ease;
        }

        .header .logout-btn:hover {
            transform: scale(1.1);
        }

        /* Sidebar */
        .sidebar {
            height: 100vh;
            width: 250px;
            position: fixed;
            top: 0;
            left: 0;
            background: linear-gradient(to bottom, #1e3c72, #2a5298);
            color: white;
            padding: 50px 0;
            overflow-y: auto;
            box-shadow: 2px 0 6px rgba(0, 0, 0, 0.1);
        }

        .sidebar .nav-link {
            color: white;
            font-size: 1rem;
            padding: 12px 30px;
            display: flex;
            align-items: center;
            gap: 10px;
            transition: all 0.3s ease;
        }

        .sidebar .nav-link:hover {
            background: rgba(255, 255, 255, 0.2);
            border-radius: 5px;
        }

        .sidebar i {
            font-size: 1.2rem;
        }

        /* Main Content */
        .content {
            margin-left: 250px;
            padding: 30px;
        }

        .content h1 {
            font-size: 2.5rem;
            color: #4facfe;
            font-weight: bold;
            margin-bottom: 20px;
        }

        .card {
            border: none;
            border-radius: 15px;
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
            overflow: hidden;
            background: linear-gradient(to bottom, #ffffff, #f8f9fa);
            transition: transform 0.3s ease;
        }

        .card:hover {
            transform: translateY(-5px);
        }

        .card img {
            height: 180px;
            object-fit: cover;
        }

        .card-body {
            text-align: center;
            padding: 20px;
        }

        .btn-primary {
            background: linear-gradient(to right, #4facfe, #00f2fe);
            border: none;
            padding: 10px 20px;
            border-radius: 10px;
        }

        .btn-primary:hover {
            background: linear-gradient(to right, #00f2fe, #4facfe);
        }

        .btn-danger {
            background: linear-gradient(to right, #f12711, #f5af19);
            border: none;
            padding: 10px 20px;
            border-radius: 10px;
        }

        .btn-danger:hover {
            background: linear-gradient(to right, #f5af19, #f12711);
        }

        .btn-warning {
            background: linear-gradient(to right, #ff9a9e, #fad0c4);
            border: none;
            padding: 10px 20px;
            border-radius: 10px;
        }

        .read-more {
            color: #4facfe;
            text-decoration: none;
            font-weight: bold;
        }

        .read-more:hover {
            color: #00f2fe;
        }

        /* Responsive Design */
        @media (max-width: 768px) {
            .sidebar {
                width: 200px;
            }

            .content {
                margin-left: 200px;
            }

            .header .logo {
                font-size: 1.5rem;
            }
        }
    </style>
</head>

<body>
    <!-- Header -->
    <div class="header">
        <div class="logo">Admin Panel</div>
        <a href="/logout" class="logout-btn">Logout</a>
    </div>

    <!-- Sidebar -->
    <div class="sidebar">
        <a href="/dashboard" class="nav-link"><i class="bi bi-speedometer2"></i> Dashboard</a>
        <a href="/add" class="nav-link"><i class="bi bi-file-earmark-plus"></i> Add product</a>
        <a href="/manageusers" class="nav-link"><i class="bi bi-people"></i> Manage Users</a>
    </div>

    <!-- Main Content -->
    <div class="content">
        <h1>Dashboard</h1>
        <p>Welcome to the Admin Panel. Manage your blogs, users, and settings here.</p>

        <div class="row g-4">
            <% user.map((val, index) => { %>
                <div class="col-md-4">
                    <div class="card">
                        <img src="<%= val.image %>" alt="Blog Image" class="w-100">
                        <div class="card-body">
                            <h5 class="card-title">product Name: <%= val.name %></h5>
                            <h5 class="card-title">product Price: <%= val.price %></h5>
                            <p>
                                <%= val.description.slice(0, 100) %>...
                                <a href="/readmore/<%= val.id %>" class="read-more">Read More</a>
                            </p>
                            <div class="d-flex justify-content-around">
                                <a href="/deletdata/<%= val.id %>" class="btn btn-danger">Delete</a>
                                <a href="/editpage/<%= val.id %>" class="btn btn-warning">Edit</a>
                            </div>
                        </div>
                    </div>
                </div>
            <% }) %>
        </div>
    </div>

    <!-- Bootstrap Icons -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap-icons/font/bootstrap-icons.css" rel="stylesheet">

    <!-- Bootstrap JS -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha3/dist/js/bootstrap.bundle.min.js"></script>
</body>

</html>
